document.addEventListener('DOMContentLoaded', function() {
    // A função M.AutoInit() vasculha a tela e inicializa todos os 
    // componentes interativos do Materialize (Selects, Modals, Dropdowns, etc).
    M.AutoInit();
    
    console.log("Sistema de comunicação da HoloRede inicializado com Materialize.");
});